{
  "name": "starter-code",
  "version": "1.0.0",
  "description": "lab-ajax-crud",
  "main": "./public/javascript/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "json-server --watch ./db.json --port 8080",
    "client": "lite-server",
    "dev": "npm run api & npm run client"
  },
  "author": "el",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0"
  }
}
